@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_Layouts.cshtml";
}

@model Model.EF.Admin

@section header
{
    Update người dùng
}

<div class="row">
    <div class="col-lg-8">
        <div class="panel panel-default">
            <div class="panel-heading">
                Nhập thông tin Admin mới
            </div>
            <div class="panel-body">
                @using (Html.BeginForm("Edit", "Admin", FormMethod.Post, new { @id = "contact_form" }))
                {
                    @Html.AntiForgeryToken()

                    <fieldset>
                        <div class="row form-group">
                            <div class="form-group">
                                <div class="col-md-3">
                                    @Html.LabelFor(model => model.Username)
                                </div>
                                <div class="col-md-7">
                                    @Html.TextBoxFor(model => model.Username,
                              new { @name = "username", @class = "form-control", @autofocus = "autofocus", @readonly = true})
                                </div>
                            </div>
                        </div>
                        <div class="row form-group">
                            <div class="form-group">
                                <div class="col-md-3">
                                    @Html.LabelFor(model => model.Password)
                                </div>
                                <div class="col-md-7">
                                    @Html.PasswordFor(model => model.Password,
                               new { @class = "form-control", @name = "password", @placeholder = "Ex: 123abc" })
                                </div>
                            </div>
                        </div>
                        <div class="col-md-7 col-md-offset-3">
                            @Html.ValidationSummary(false, "", new { @class = "text-danger" })
                        </div>
                        <!-- Change this to a button or input when using this as a form -->
                        <div class="col-md-3 col-md-offset-4">
                            <button type="submit" class="btn btn-md btn-success btn-block">Cập nhật</button>
                        </div>
                    </fieldset>

                }
            </div>

        </div>
    </div>
</div>


